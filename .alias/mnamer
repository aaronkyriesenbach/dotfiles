rename-movie () {
    destination="${2:-/mnt/data/movies}"

    mnamer --movie-directory "$destination/{name} ({year})" "$1"
}

rename-tv () {
    destination="${2:-/mnt/data/tv}"

    mnamer --episode-directory "$destination/{series}/Season {season}" "$1"
}
